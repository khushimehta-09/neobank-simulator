import React,{useEffect,useState}from'react';import{Trophy,RefreshCw,BarChart3}from'lucide-react';import api from'../services/api';export default function Leaderboard(){const[stats,setStats]=useState(null);const load=async()=>{try{setStats((await api.get('/social/stats')).data)}catch(e){alert('Could not load leaderboard')}};useEffect(()=>{load()},[]);return <div className='space-y-6'><div className='flex flex-col md:flex-row md:items-end justify-between gap-4'><div><h1 className='text-3xl font-black flex gap-3'><Trophy className='text-warning'/>Leaderboard</h1><p className='text-text-muted text-sm'>Ranks users using game, saving/balance, transfer activity, trust score, XP and social feed activity.</p></div><button onClick={load} className='p-3 bg-white/5 rounded-xl self-start md:self-auto'><RefreshCw/></button></div><div className='space-y-3'>{(stats?.topUsers||[]).map((u,i)=><div key={u.id} className='glass-panel p-4 rounded-2xl grid grid-cols-1 md:grid-cols-[60px_1fr_auto] gap-4 items-center'><b className='text-2xl'>#{i+1}</b><div><p className='font-black text-lg'>{u.name}</p><div className='grid grid-cols-2 md:grid-cols-5 gap-2 mt-2 text-xs text-text-muted'><span>Level {u.level}</span><span>XP {u.xp}</span><span>Trust {u.trustScore}</span><span>Tx {u.transactionCount}</span><span>Social {u.socialCount}</span></div></div><div className='text-primary font-black text-right'><BarChart3 className='inline mr-1' size={18}/>{u.totalScore} pts</div></div>)}</div>{stats?.recentActivity?.length>0&&<div className='glass-panel p-5 rounded-2xl'><h2 className='font-black mb-3'>Recent Social Feed Activity</h2>{stats.recentActivity.map(a=><p key={a.id} className='text-sm py-2 border-b border-white/5 last:border-0'><b>{a.name}</b> — {a.title}</p>)}</div>}</div>}
